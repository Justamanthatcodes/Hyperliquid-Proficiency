from hyperliquid.info import Info
from hyperliquid.utils import constants

info = Info(constants.MAINNET_API_URL, skip_ws=True)

user = info.user_state(address)
#Tels you whats happening in the walelt - Withdrawable amount, Asset positions

user2 = info.open_orders(address) 
#Shows you orders that haven't been filled yet
